# Docker Compose identity for mounted directories
PUID=1000
PGID=1000
TZ=Asia/Shanghai

# Set true when the service is behind an HTTPS reverse proxy.
COOKIE_SECURE=false

# aria2 BitTorrent/DHT inbound port (mapped over TCP and UDP).
ARIA2_P2P_PORT=51413

